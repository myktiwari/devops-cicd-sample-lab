version: 0.0
os: linux
files:
  - source: app/
    destination: /var/www/simple-node-app/app
hooks:
  BeforeInstall:
    - location: scripts/stop_server.sh
      timeout: 300
      runas: root
  AfterInstall:
    - location: scripts/start_server.sh
      timeout: 300
      runas: root
