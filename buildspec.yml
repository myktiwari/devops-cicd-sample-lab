version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
  build:
    commands:
      - echo "Building Node.js app..."
      - mkdir -p build
      - cp -r * build/
      - cd build && zip -r ../app.zip .
artifacts:
  files:
    - app.zip
  discard-paths: yes
